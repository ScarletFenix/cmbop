; Override snippet only — not a full php.ini. Hostinger MultiPHP merges keys.
; Do not cap RAM or script time (can lower host defaults).
[PHP]
max_input_vars = 10000
post_max_size = 64M
upload_max_filesize = 64M
max_input_time = 120
pcre.backtrack_limit = 10000000
pcre.recursion_limit = 100000
