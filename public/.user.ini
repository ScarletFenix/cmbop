; Article .docx uploads are capped at 5 MB in the app; PHP must allow at least that
; (shared hosts often default to 2M, which would reject a 5 MB Word file).
max_input_vars = 10000
post_max_size = 64M
upload_max_filesize = 16M
