; Same PHP limits as public/.user.ini. Some Hostinger layouts scan the
; Laravel root (one level above the front controller).
max_input_vars = 10000
post_max_size = 64M
upload_max_filesize = 64M
max_input_time = 120
max_execution_time = 120
memory_limit = 256M
